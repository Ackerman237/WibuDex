# Archive

Folder ini berisi dokumen lama yang sudah tidak aktif digunakan.

Semua konten di sini telah dimigrasikan ke sistem dokumentasi terstruktur
di folder `docs/` (folder `01-` s.d. `08-`).

| Folder / File | Digantikan oleh |
|---|---|
| `Rancangan-project/` | Seluruh sistem folder `docs/` |
| `rencana-restrukturisasi-wibudex.md` | Sudah dieksekusi — catatan historis |

**Jangan update dokumen di sini.** Untuk referensi aktif, gunakan
folder `docs/` langsung.
